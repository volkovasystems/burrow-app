#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../btoa-atob/btoa" "$@"
  ret=$?
else 
  node  "$basedir/../btoa-atob/btoa" "$@"
  ret=$?
fi
exit $ret
